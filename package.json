{
  "name": "next-nest-graphql-antd-monorepo",
  "private": true,
  "packageManager": "pnpm@9.6.0",
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": ["@nestjs/common", "@nestjs/core"]
    }
  },
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.0.6",
    "typescript": "^5.6.2"
  }
}
